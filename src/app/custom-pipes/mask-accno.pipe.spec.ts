import { MaskAccnoPipe } from './mask-accno.pipe';

describe('MaskAccnoPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskAccnoPipe();
    expect(pipe).toBeTruthy();
  });
});
