import { PrependTitlePipe } from './prepend-title.pipe';

describe('PrependTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PrependTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
