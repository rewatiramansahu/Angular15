import { CustomInputDirectiveDirective } from './custom-input-directive.directive';

describe('CustomInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
