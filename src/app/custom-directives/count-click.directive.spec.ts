import { CountClickDirective } from './count-click.directive';

describe('CountClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CountClickDirective();
    expect(directive).toBeTruthy();
  });
});
