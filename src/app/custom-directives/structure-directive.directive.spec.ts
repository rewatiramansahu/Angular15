import { StructureDirectiveDirective } from './structure-directive.directive';

describe('StructureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StructureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
